Problem 2:
    The reason rand_r is thread safe is because it uses a independent seed variable which prevents race condition as it doesn't use a global or static state which is shared between threads.

