3.1-
An example of a operation which only changes a regular files last status change is the chmod command.

3.2-
The ehco command changes the modify time for example the command "echo "adding this text" >> test.txt".

3.3-
The touch command changes the access time as well as the modify and changes.

3.4-
An application such as an image uploader must recongize the max length of a file name for cloud services which have a max on the length of a file name.

